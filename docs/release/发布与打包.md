# 发布与打包

这份文档聚焦 **发布者 / 维护者视角** 的打包说明。

如果你只是想从源码运行，或者先生成一个本地体验包，请优先阅读：

- [从源码构建与打包](从源码构建与打包.md)

## 打包模式

Caps Nav 当前提供两种打包模式：

- `dev`
  - 用于本地测试分发
  - 基于 `Release` 配置构建
  - 不包含 `Developer ID` 正式签名、公证和票据贴附
- `release`
  - 用于正式发布，包含签名、公证和最终验证

这里要特别注意：

- `dev` 不是 `Debug` 包
- `dev` 也不是 Xcode 意义上的“开发包”
- 它更准确地说，是一个“未完成正式签名与公证的 Release 级本地分发包”

## 目标产物

当前脚本会生成：

- `Universal` 通用版 App
  - 同时包含 `arm64` 与 `x86_64`
- 一个可分发的安装包（`.dmg`）
- 一个对应的 `SHA-256` 摘要文件

最低系统版本仍受工程配置约束，目前为：

- `macOS 13.0+`

## 前提条件

执行打包前，建议先确认：

- 本机已安装 Xcode 和 Command Line Tools（命令行开发工具）
- 当前工程能正常构建
- 可以执行 `xcodebuild`
- 可以执行 `swift`
- 系统可用 `hdiutil`、`ditto`、`shasum`、`osascript`

如果你要执行 `release` 模式，还需要：

- 钥匙串里已安装可用的 `Developer ID Application` 证书（Apple 开发者分发签名证书）
- 已配置 `xcrun notarytool` 的 keychain profile（公证凭据配置）
- 本机可用 `codesign`、`xcrun`、`spctl`

## 一条命令打包

### `dev` 模式

```bash
./tools/package.sh --mode dev
```

这个模式适合：

- 你自己本机验证安装流程
- 发给少量测试用户体验功能
- 还没有配置 Apple 开发者正式分发能力时先把安装包产出来

脚本会自动完成：

1. 构建 `Release`
2. 生成 `arm64 + x86_64` 通用版 App
3. 校验双架构
4. 生成视觉化安装包（DMG）
5. 生成 `.dmg.sha256`

默认输出目录：

```text
artifacts/dev/
```

### `release` 模式

```bash
./tools/package.sh --mode release \
  --signing-identity "Developer ID Application: Example Corp (TEAM123456)" \
  --team-id "TEAM123456" \
  --notary-profile "capsnav-release"
```

脚本会自动完成：

1. `xcodebuild archive`
2. 生成 `ExportOptions.plist`
3. `xcodebuild -exportArchive`
4. 校验签名后的导出 App
5. 生成最终安装包（DMG）
6. `xcrun notarytool submit --wait`
7. `xcrun stapler staple`
8. `xcrun stapler validate`
9. `spctl` 最终验证
10. 生成 `.dmg.sha256`

## 常用参数

### 自定义输出目录

```bash
./tools/package.sh --mode dev --output-dir /absolute/path/to/output
```

### 自定义 DerivedData 目录

```bash
./tools/package.sh --mode dev --derived-data /absolute/path/to/.build/ReleasePackaging
```

### 跳过 SHA-256 摘要

```bash
./tools/package.sh --mode dev --skip-sha256
```

## `release` 模式的参数来源

`release` 模式必须拿到这三个值：

- `signing identity`
- `team id`
- `notary profile`（公证凭据名称）

脚本读取优先级是：

1. 命令行参数
2. 环境变量
3. 缺失时报错退出

对应环境变量：

```bash
export CAPS_NAV_SIGNING_IDENTITY="Developer ID Application: Example Corp (TEAM123456)"
export CAPS_NAV_TEAM_ID="TEAM123456"
export CAPS_NAV_NOTARY_PROFILE="capsnav-release"
```

然后可以直接执行：

```bash
./tools/package.sh --mode release
```

## `notarytool` 凭据建议

建议先在发布机器上保存一次凭据：

```bash
xcrun notarytool store-credentials "capsnav-release" \
  --apple-id "your-account@example.com" \
  --team-id "TEAM123456" \
  --password "app-specific-password"
```

之后脚本里只传 `--notary-profile` 即可，不要把真实 Apple ID 和密码写进仓库。

## GitHub Actions 自动生成 dev 预发布

如果你想在 GitHub 上自动生成一个可下载的 `dev` 安装包，仓库已经提供：

- [`.github/workflows/pre-release.yml`](../../.github/workflows/pre-release.yml)

这条工作流的定位是：

- 自动读取当前工程版本号
- 生成 `v版本号` 的 GitHub Release
- 将它标记为 `pre-release`
- 自动上传 `DMG` 和 `SHA-256` 文件

这里生成的仍然是 `dev` 包，也就是：

- 基于 `Release` 配置构建
- 不依赖本地 Apple ID 登录
- 不要求 Apple Developer Program 年费账号
- 不包含 `Developer ID` 正式签名、公证和票据贴附
- 由于运行在无头环境，安装包不会包含 Finder 视觉布局

### 这条 pre-release 工作流的缺点

这条工作流的目标是“稳定自动出包”，不是“生成最终可公开分发的正式安装盘”，所以它有这些限制：

- 第一次打开时，macOS 很可能弹出安全拦截提示
- 用户需要手动到“系统设置 -> 隐私与安全性 -> 安全性”中选择“仍要打开”
- 安装包使用简化 DMG，不包含背景图、图标摆位和拖拽安装视觉提示
- 它适合提前体验、测试、灰度分发，不适合作为最终正式发布形态

用户第一次打开时，常见拦截提示大致如下：

![首次打开安全提示示意图](assets/%E9%A6%96%E6%AC%A1%E6%89%93%E5%BC%80%E5%AE%89%E5%85%A8%E6%8F%90%E7%A4%BA%E7%A4%BA%E6%84%8F%E5%9B%BE.png)

### 使用方式

1. 先在工程里更新版本号
2. 推送代码到 GitHub 默认分支
3. 打开仓库的 `Actions`
4. 运行 `Build Pre-Release`
5. 等待工作流完成
6. 到仓库的 `Releases` 页面下载产物

### 版本号与标签规则

- 工作流不会手动输入版本号
- 它会自动读取工程里的 `MARKETING_VERSION`
- 自动创建同名 tag 和 release，例如：
  - `v0.0.1`
  - `v0.0.2`

如果同名 tag 已经存在，工作流会直接失败，并提示你先更新工程版本号。

### 自动生成的 Release 说明

工作流会在 Release 顶部补一段固定说明，用来告诉用户：

- 这是 `dev` 预发布安装包
- 它基于 `Release` 构建
- 它没有经过正式签名和公证
- 第一次打开时，macOS 可能拦截
- 当前安装包不包含视觉化 DMG 布局

其余更新内容会使用 GitHub 自动生成的 Release Notes。

## 产物命名

当前产物命名格式为：

```text
Caps-Nav-<版本号>-<构建号>-macOS-universal.dmg
```

例如：

```text
Caps-Nav-1.0-1-macOS-universal.dmg
```

对应摘要文件：

```text
Caps-Nav-1.0-1-macOS-universal.dmg.sha256
```

## 安装包（DMG）内容

生成的安装包默认包含：

- `Caps Nav.app`
- `Applications` 快捷方式

用户打开安装包后，可以直接把 App 拖到“应用程序”目录。

## 视觉化安装盘

当前安装包会额外完成这些处理：

- 自动生成品牌化背景图
- 设置访达（Finder）窗口尺寸
- 固定 `Caps Nav.app` 与 `Applications` 的摆位
- 显示中文拖拽安装提示

相关实现文件：

- `tools/package.sh`
- `tools/GenerateDMGBackground.swift`

补充说明：

- 本地正常打包时，会生成这套视觉化 DMG
- GitHub Actions 的 `pre-release` 工作流使用 `--headless-dmg`
- 因此 GitHub 自动产出的 `pre-release` 只保留基础安装能力，不包含这套视觉效果

## 建议的发布后检查

打包完成后，建议至少手工检查：

1. 安装包（DMG）能正常打开
2. `Caps Nav.app` 和 `Applications` 图标都存在
3. App 拖入“应用程序”后能正常启动
4. 首次启动后菜单栏图标正常出现
5. 权限申请、设置页和前缀键监听都能正常工作

如果你想额外确认架构，可以执行：

```bash
lipo -info '/path/to/Caps Nav.app/Contents/MacOS/Caps Nav'
```

预期输出中应同时出现：

- `arm64`
- `x86_64`

## 对外分发时的说明建议

如果你分发的是 `dev` 模式产物，需要提前告诉用户：

- 第一次打开时，macOS 可能拦截
- 需要到“系统设置 -> 隐私与安全性”中选择“仍要打开”
- 更准确地说，通常是在“系统设置 -> 隐私与安全性 -> 安全性”区域看到放行入口
- 如果用户看到“Apple 无法验证 `Caps Nav.app` 是否包含可能危害 Mac 安全或泄露隐私的恶意软件”，这属于当前预发布包的预期现象
- GitHub 自动生成的预发布 DMG 不包含品牌化背景图和图标布局
- 它虽然是 `Release` 构建，但还不是经过 Apple 正式签名和公证的公开发布包

可以直接给用户这段操作说明：

1. 在拦截弹窗里先点“完成”
2. 打开“系统设置”
3. 进入“隐私与安全性”
4. 在“安全性”区域找到 `Caps Nav.app` 的提示
5. 点击“仍要打开”
6. 再次打开应用，并在确认弹窗中继续启动

如果你分发的是 `release` 模式产物，仍建议用户：

- 首次启动后先完成辅助功能权限授权
- 再去测试默认键位和配置方案功能
